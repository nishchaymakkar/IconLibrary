{
  "name": "icon-library",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare-icons": "node scripts/copy-icons.js",
    "dev": "npm run prepare-icons && vite",
    "build": "npm run prepare-icons && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-window": "^1.8.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  }
}
